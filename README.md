# Radar ONE  
**Интерактивная онлайн‑карта угроз в реальном времени**  
(события в России и новых регионах, получаемые из Telegram‑каналов, обрабатываемые LLM‑моделью и доставляемые подписчикам через WebSocket и Telegram‑бота).

## 📚 Содержание  

1. [Общее описание](#-общее-описание)  
2. [Ключевые возможности](#-ключевые-возможности)  
3. [Архитектура проекта](#-архитектура-проекта)  
4. [Быстрый старт (Docker)](#-быстрый-старт-docker)  
5. [Запуск без Docker](#-запуск-без-docker)  
6. [Конфигурация и переменные окружения](#-конфигурация-и-переменные-окружения)  
7. [База данных](#-база-данных)  
8. [Backend (FastAPI)](#-backend-fastapi)  
9. [Frontend (карта и уведомления)](#-frontend)  
10. [Telegram‑бот](#-telegram-бот)  
11. [Логи](#-логи)  
12. [Разработка и вклад в проект](#-разработка)  
13. [Лицензия](#-лицензия)  
14. [Контакты](#-контакты)  

## 🌐 Общее описание  

Radar ONE собирает сообщения из публичных Telegram‑каналов, в которых публикуются сообщения о воздушных, ракетных и иных угрозах.  
Текст сообщения проходит через LLM‑модель (OpenAI GPT o1‑mini **или** Ollama), откуда извлекаются:

* **Регион** – точное официальное название субъекта РФ/оккупированного региона.  
* **Тип угрозы** – `UAV`, `AIR`, `ROCKET`, `UB` (бесэкипажный катер) или `ALL`.  
* **Статус** – `HD` (высокий), `MD` (средний) или `AC` (отбой/нет угрозы).

Полученные данные сохраняются в PostgreSQL, транслируются подписчикам через:

* **WebSocket** – реальное обновление карты и нотификации.  
* **Telegram‑бот** – push‑уведомления и возможность самостоятельного сообщения о новых угрозах.

## 🚀 Ключевые возможности  

| Что | Как реализовано |
|-----|----------------|
| **Реальное время** | Scraper (`listener.py`) проверяет Telegram‑каналы каждые 10 сек, а PostgreSQL уведомляет через `LISTEN/NOTIFY`. |
| **Автоматический анализ** | LLM‑модель (`analyzer.py`) формирует стандартный вывод `СТАТУС/РЕГИОН/ТИП`. При недоступности OpenAI используется локальная модель Ollama. |
| **Подписки по регионам** | Telegram‑бот сохраняет подписки в таблице `subscriptions`. |
| **Интерактивная карта** | Frontend (MapLibre GL) отображает статус каждой области, меняет цвет в зависимости от уровня угрозы. |
| **WebSocket‑интерфейс** | Пакеты `snapshot` (полный статус) и `region_update` (обновление отдельного региона). |
| **Уведомления в Telegram** | Форматированный HTML‑сообщения (`notifications.format_notification`). |
| **Управление пользователями** | Администраторы (переменная `ADMIN_USER_ID`) могут банить/разбанивать, рассылать сообщения, публиковать отчёты. |
| **Логирование** | `logger.py` пишет в консоль и в файлы `logs/radarone.log` (ежедневная ротация, 30 дней). |
| **Docker‑композит** | Один `docker-compose.yml` разворачивает backend, frontend, Nginx‑reverse‑proxy, PostgreSQL и Portainer. |

## 🏗️ Архитектура проекта  

```
+-----------------+        +------------------+        +--------------------+
| Telegram-каналы |  --->  |    listener.py   |  --->  |    analyzer.py     |
+-----------------+        +------------------+        +--------------------+
                                    |                            |
                                    v                            v
                           +------------------+        +---------+----------+
                           |    PostgreSQL    | <----> |   db.py (asyncpg)  |
                           +------------------+        +---------+----------+
                                    |                            |
                                    |                            |
                        +-----------+-----------+    +-----------+-----------+
                        |   FastAPI (backend)   |    |      Telegram‑бот     |
                        +-----------+-----------+    +-----------+-----------+
                                    |                            |
                                    v                            v
                            +-------+--------+          +--------+--------+
                            |  WebSocket WS  |          |  Bot API (poll) |
                            +-------+--------+          +-----------------+
                                    |
                                    v
                          +---------+---------+
                          |  Frontend (NGINX) |
                          |  index.html + js  |
                          +-------------------+

```

* `listener.py` – парсит публичные каналы, передаёт текст `process_message`.  
* `analyzer.py` – LLM‑анализ, fallback к Ollama.  
* `db.py` – асинхронный слой над PostgreSQL, создает таблицы при первом запуске.  
* `main.py` – FastAPI‑сервер: HTTP‑API `/api/statuses`, WebSocket `/ws`, фоновые задачи (listener, fallback‑poll, PG‑listen, бот‑в‑отдельном‑потоке).  
* `bot.py` – telegram‑бот, команды подписки/отчетов/администрирования.  
* `frontend` – статические файлы, подключённые к `/ws`, визуализируют данные на карте.

## 📦 Быстрый старт (Docker)

> **Требуется:** Docker ≥ 20.10, Docker‑Compose ≥ 2.0 (встроенный в Docker Desktop).

```bash
# 1. Клонировать репозиторий
git clone https://github.com/justarist/radarone.git
cd radarone

# 2. Скопировать пример .env и заполнить свои значения
cp .env.example .env   # если файла нет, создайте его вручную (см. ниже)

# 3. Запустить всё в контейнерах
docker compose up -d   # или: docker-compose up -d

# 4. После успешного старта:
#    - Backend API   : http://localhost/api/statuses
#    - Карта (frontend) : http://localhost
#    - WebSocket endpoint (для отладки) : ws://localhost/ws
```

Остановить всё:

```bash
docker compose down
```

### Структура Docker‑контейнеров  

| Сервис          | Образ / Dockerfile | Порты | Назначение |
|-----------------|-------------------|-------|------------|
| `backend`       | `python:3.13‑slim` (см. `backend/Dockerfile`) | 8000 (внутренний) | FastAPI + listener + bot‑thread |
| `frontend`      | `nginx:alpine` (см. `frontend/Dockerfile`) | 80 (внутренний) | Статические файлы (HTML, CSS, JS) |
| `nginx‑proxy`   | `nginx:alpine` | 80 / 443 → хост | Обратный прокси, TLS (Let's Encrypt) |
| `postgres`      | `postgres:16`    | 5432  | Хранилище сообщений об атаках |
| `portainer`     | `portainer/portainer-ce` | 9000 | UI‑интерфейс для управления Docker‑инфраструктурой |

## 🛠️ Запуск без Docker (для разработки)

```bash
# 1. Установить Python (>=3.13) и PostgreSQL
# 2. Клонировать репозиторий
git clone https://github.com/justarist/radarone.git
cd radarone

# 3. Создать виртуальное окружение и установить зависимости
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 4. Создать .env (см. ниже)

# 5. Запустить PostgreSQL (можно в Docker, или локально)
#    Убедитесь, что база `attacks` доступна.

# 6. Запустить backend
uvicorn main:app --host 0.0.0.0 --port 8000

# 7. Открыть index.html в браузере (можно через простой http‑server):
cd frontend
python -m http.server 8080   # теперь карта доступна по http://localhost:8080
```

## 🔧 Конфигурация и переменные окружения  

### Пример `.env`

```dotenv
# ------------------ Server -------------------

HOST=0.0.0.0
PORT=8000
POLL_FALLBACK_SEC=15  # polling‑fallback, сек

# ------------------ DB -----------------------

DB_USER=postgres
DB_PASSWORD=760942
DB_NAME=attacks
DB_HOST=postgres  # имя контейнера в docker‑compose, иначе localhost
DB_PORT=5432

# ------------------ Telegram -----------------

BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11  # ваш токен бота
ADMIN_USER_ID=111111111,222222222  # ID администраторов (через запятую)

# ------------------ LLM ----------------------

OPENAI_API_KEY=sk-****************************************  # если используете OpenAI
OLLAMA_API_KEY=YOUR_OLLAMA_API_KEY  # если используете Ollama
OLLAMA_MODEL=gemma:2b  # пример, может быть любой доступный

```

#### О переменных  

| Переменная | Описание |
|-------------|----------|
| `HOST`, `PORT` | На каком IP/порту слушать FastAPI. |
| `POLL_FALLBACK_SEC` | Период (сек) fallback‑poll'а, если `LISTEN` недоступен. |
| `DB_*` | Параметры подключения к PostgreSQL. |
| `BOT_TOKEN` | Токен Telegram‑бота (используется для рассылок и админ‑команд). |
| `ADMIN_USER_ID` | Список Telegram‑ID администраторов, разделённый запятыми. |
| `OPENAI_API_KEY` | Ключ OpenAI (если хотите использовать GPT‑o1‑mini). |
| `OLLAMA_API_KEY` | Токен доступа к Ollama‑cloud (если используете Ollama). |
| `OLLAMA_MODEL` | Название модели в Ollama, напр. `gemma:2b` либо `llama3`. |

> **Важно:** При работе без Docker `DB_HOST` обычно `localhost`. В Docker‑compose оставьте `postgres`, т.к. это имя сервиса в сети `radarnet`.

### Региональные данные (`config.py`)

* `region_names` – список официальных названий всех поддерживаемых регионов (включая `"Россия"` — регион, использующийся для выдачи угроз сразу во всех регионах РФ). Добавляйте/удаляйте записи тут, а затем перезапустите сервис.
* `telegram_channels` – массив публичных Telegram‑каналов, откуда будет читаться информация.  
  Чтобы добавить новый канал, просто допишите его название (без `@`) в массив.

## 📂 База данных  

Таблицы создаются автоматически при первом подключении (`db._init_schema`). Ниже их краткое описание.

| Таблица | Поля | Описание |
|---------|------|----------|
| `attacks` | `id` (PK), `region`, `attack_type`, `status`, `source`, `timestamp` | События атак/угроз. |
| `subscriptions` | `id` (PK), `user_id`, `region`, `is_banned` (bool) | Подписки пользователей и их статус бана. |
| **Триггер** `attack_insert_trigger` |  | После `INSERT` в `attacks` генерирует `pg_notify('attack_updates', row_to_json(NEW)::text)`. |

**Индексы**: `idx_region_attack_type` ускоряет запросы поиска последних статусов по региону/типу.

## ⚡ Backend (FastAPI)

### HTTP API  

| Метод | Путь | Описание | Пример ответа |
|-------|------|----------|---------------|
| `GET` | `/api/statuses` | Возвращает текущие статусы всех регионов в виде `{region: {attack_type: status}}`. | ```json { "Московская область": { "UAV": "HD", "AIR": "MD" }, "Крым": { "UB": "AC" } }``` |
| `GET` | `/` | (неявно) — только справка FastAPI. |
| **WebSocket** | `/ws` | После подключения клиент получает **snapshot** (полный текущий статус). Далее сервер отправляет сообщения: <br> *`type: "region_update"`* – обновление конкретного региона; <br> *`type: "snapshot"`* – полностью новый снапшот (при старте). | ```json { "type": "region_update", "data": { "region": "Крым", "statuses": { "UAV": "HD", "AIR": "MD", "ROCKET": "AC" } } }``` |

### Запуск (если не используете Docker)

```bash
uvicorn main:app --host $HOST --port $PORT
```

> При старте сервис автоматически запустит:  
> * `listener.listener_loop` – парсит Telegram‑каналы (по умолчанию каждые 10 сек).  
> * `listener.poll_and_broadcast_loop` – fallback‑poll, если `LISTEN` не работает.  
> * `listener.pg_listen_and_forward` – подписка на `PG_NOTIFY_CHANNEL`.  
> * `bot.main()` – запускает Telegram‑бота в отдельном потоке.

## 🎨 Frontend (карта)

* **HTML** – `frontend/index.html`.  
* **JS‑модули** – `js/main.js`, `js/map.js`, `js/notifications.js`, `js/ws.js`, `js/utils.js`, `js/menu.js`.  
* **CSS** – `css/styles.css`.  

### Как это работает

1. При загрузке `main.js` инициализируется карта (`maplibre-gl`) и открывается WebSocket к `ws(s)://HOST/ws`.  
2. При первом подключении сервер рассылает **snapshot** со статусами всех регионов.  
3. При получении `region_update` вызывается `updateRegionStatus`, которая:  
   * Обновляет локальный объект `REGION_STATUS`.  
   * Перерисовывает цвет соответствующего полигона.  
   * Вызывает `showNotification` (показ всплывающего сообщения).  

### Текстовое сообщение (frontend)

Формат сообщения в UI (текст HTML) формируется функциями из `utils.js`:  
* `formatNotification` – перевод кода статуса/типа угрозы в человекочитаемый русский текст.  
* `getColorByStatus` – цветовая шкала: `HD` → красный, `MD` → жёлтый, `AC` → зелёный.  

### Кастомизация

* Чтобы изменить стили уведомлений – правьте `css/styles.css`.  
* Чтобы добавить новые типы атак, отредактируйте `utils.formatNotification` и `utils.getColorByStatus`.  

## 📱 Telegram‑бот

Бот реализован в `bot.py` и использует **python‑telegram‑bot** (версия 20+).

### Основные команды

| Команда | Параметры | Описание |
|---------|-----------|----------|
| `/start` | – | Приветственное сообщение, инструкция. |
| `/help` | – | Список всех команд. |
| `/status <регион>` | Регион | Последние 5 событий в указанном регионе. |
| `/subscribe <регион>` | Регион / `all` | Подписаться на уведомления (по отдельному региону либо на все). |
| `/unsubscribe <регион>` | Регион / `all` | Снять подписку. |
| `/subscriptions` | – | Показать текущие подписки. |
| `/report` | – | Открывает диалог для отправки собственного сообщения о угрозе (модерация). |
| `/channels` | – | Список Telegram‑каналов, из которых берётся информация. |
| `/about` | – | Информация о проекте. |
| **Админ** `/ban <user_id> <причина>` | – | Банит пользователя (запрещает `/report`). |
| **Админ** `/unban <user_id> <причина>` | – | Снимает бан. |
| **Админ** `/is_banned <user_id>` | – | Проверка статуса. |
| **Админ** `/admin_report <сообщение>;<комментарий>` | – | Отправляет сообщение в систему от имени администратора (без модерации). |
| **Админ** `/admin_message <текст>` | – | Широковещательная рассылка всем пользователям. |

> **Важно:** Чтобы стать администратором, укажите свой Telegram‑ID в переменной окружения `ADMIN_USER_ID` (через запятую).

### Как работает `/report`

1. Пользователь вводит сообщение (в свободной форме).  
2. Бот пересылает его в личный чат каждому администратору, где появляется клавиатура «✅ Approve / ❌ Reject».  
3. При подтверждении (`Approve`) сообщение отправляется в `process_message` (тот же pipeline, что и из каналов).  

## 📁 Логи  

`logger.py` использует стандартный `logging`:

* **Консоль** – отладочный уровень `INFO`.  
* **Файлы** – `logs/radarone.log` с ротацией каждый полночь, хранится 30 дней.  
* **Фильтр EmojiStripFilter** – убирает эмодзи из записей в логи (чтобы не засорять файлы).  

Для изменения уровня логирования измените `logger.setLevel(logging.INFO)` в `logger.py` (на `DEBUG`, `WARNING`, …).

## 🛠️ Разработка  

### Ветка разработки

* `main` – стабильная версия, используемая в продакшене.  
* Для новых функций создавайте ветки `feature/*` и открывайте Pull Request.  

### Как добавить новый регион / тип угрозы  

1. **Регион** – добавить имя в `config.region_names`.  
2. **Территориальная привязка** – при необходимости добавить сопоставление в `map.js` (`nameMap`).  
3. **Тип угрозы** – обновить `utils.formatNotification`, `utils.getColorByStatus`, и при необходимости `allowedUB` в `map.js`.  

### Как добавить новый Telegram‑канал  

```python
# config.py
telegram_channels = [
    "radarrussiia",
    "bidengoy",
    "RDFradar",
    "lpr1_Kherson_alarm",
    "my_new_channel",  # ← добавить сюда
]
```

Перезапустите сервис `backend` – новые каналы начнут скрейпиться автоматически.

## 📜 Лицензия  

Проект распространяется под лицензией **MIT**. Полный текст лицензии находится в файле [LICENSE](LICENSE).  

## ☎️ Контакты

* **Telegram‑канал проекта** – https://t.me/radaroneteam  
* **Telegram‑бот** – https://t.me/radaronebot  
* **GitHub репозиторий** – https://github.com/justarist/radarone
* **Поддержать донатом** – https://radarone.online/donate

## ✍️ Авторы

* Автор, главный разработчик (Backend + Frontend + nginx + Docker) – [JustArist](https://github.com/justarist)
* Соавтор, Backend-разработчик (listener.py и др.) – [perlch](https://github.com/perlch)
